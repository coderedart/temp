on: push
  
name: CI
jobs:
  build:
    runs-on: macos-latest
    # runs-on: ${{ matrix.os }}-latest
    # strategy:
    #   matrix:
    #     os: [ubuntu, windows, macos]
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Clang
        shell: bash
        run: |
          clang main.c
          
          

      

